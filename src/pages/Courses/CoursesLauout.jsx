import { H } from '../../ui-components';
import { SectionFirstContainer } from './components';
import styled from './Courses.module.css';

export const CoursesLayout = () => {
	return (
		<div className={styled.wrapper}>
			<SectionFirstContainer />
		</div>
	);
};
